package com.mysite.snap.repository;

import com.mysite.snap.entity.SnapRemove;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SnapRemoveRepository extends JpaRepository<SnapRemove, Long> {
}
